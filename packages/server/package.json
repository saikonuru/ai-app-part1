{
  "name": "server",
  "scripts": {
    "build": "echo \"Server build step (if any)\"",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "prisma": "^6.16.2"
  }
}
