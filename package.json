{
  "name": "ai-app",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "echo 'Root build script'",
    "dev": "bun run index.ts",
    "debug": "bun --inspect run index.ts",
    "format": "prettier --write .",
    "prepare": "husky"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.1.5",
    "prettier": "^3.6.2"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "react-hook-form": "^7.62.0"
  }
}
